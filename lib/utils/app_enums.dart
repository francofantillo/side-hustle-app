enum PaymentTypeEnum { Cash, Card }

enum AttendeesEnum { Interested, Confirmed , Rejected}

enum SideHustleType { Products, Services }

enum DeliveryOptions { Pickup, DeliverToAddress_COD }

enum Favourites { Event, Job, Shop }

enum SubscriptionPlan { One, Two, Three }

enum Events { Scheduled, Ongoing, Completed }

// enum Attendees { Interested, Confrimed }
